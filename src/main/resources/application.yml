server:
  port: 8086

spring:
  # H2 in-memory database for development
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      jakarta:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
          format_sql: true

  h2:
    console:
      enabled: true

  # Ensure Redis is completely disabled for dev
  redis:
    enabled: false

  # Explicitly set active profile
  profiles:
    active: dev